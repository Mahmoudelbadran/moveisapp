import 'package:flutter/material.dart';

class RondomView extends StatelessWidget {
  const RondomView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}